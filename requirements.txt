selenium>=4.18.0
beautifulsoup4>=4.12.0
requests>=2.31.0
streamlit==1.57.0
# pandas 3.x'in Arrow-string motoru Streamlit thread'lerinde SEGFAULT (exit 139)
# üretiyor (10.07.2026, faulthandler kanıtı: string_arrow._from_sequence ve
# Series.map'te iki ayrı çökme). 2.x'e sabit — 3.x'e geçiş bilinçli test ister.
pandas>=2.2.0,<3.0
# pyarrow da ayni sebepten sabit: tavansiz birakinca Render taze build'de
# en yeni surumu kurdu ve st.data_editor'un DataFrame->Arrow cevrimi
# (pyarrow pandas_compat.convert_column) SEGFAULT verdi (17.07.2026,
# faulthandler kaniti: app Internal Scout SWOT data_editor'u). 19.x hatti
# pandas 2.2 ile uzun sure test edilmis durumda.
pyarrow>=14,<20
lxml>=5.2.0
plotly>=5.20.0
bcrypt>=4.0.0
streamlit-authenticator>=0.3.3
openpyxl>=3.1.0
gspread>=6.0.0
google-auth>=2.0.0
fpdf2>=2.7.0
streamlit-cookies-controller>=0.0.4
deep-translator>=1.11.0
